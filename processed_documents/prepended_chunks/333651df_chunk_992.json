{
  "content": "The provided chunk consists of methods for managing and controlling multiple animation panels (actor controls) within an application. These methods enable, disable, reset, and synchronize playback across all connected animation panels. The context is within a Python class `AnimPanel` that manages animations for one or more actors, allowing synchronized control over their playback states.\n\n```python\nclass AnimPanel:\n    def __init__(self):\n        # Initializes the anim panel with controls for multiple actors\n\n    def addActorControl(self, actor):\n        # Adds an actor's controls to the panel\n\n    # [Other existing methods]\n\n    <chunk>\n    def enableActorControls(self):  # Enable all actor controls\n    def disableActorControls(self):  # Disable all actor controls\n    def disableActorControlAt(self, index):  # Disable a specific control at index\n    def displayFrameCounts(self):  # Show frame counts for all controls\n    def displaySeconds(self):  # Show time in seconds for all controls\n    def resetAllToZero(self):  # Reset all actors to start of animation\n    def resetAllToEnd(self):  # Reset all actors to end of animation\n    def nextFrame(self):  # Advance playback by one frame for all actors\n    </chunk>\n\n    def updateAllGuiElements(self):\n        # Updates GUI elements for all actor controls\n```\n\ndef enableActorControls(self):\n        for actorControl in self.actorControlList:\n            actorControl.enableControl()\n\n    def disableActorControls(self):\n        for actorControl in self.actorControlList:\n            actorControl.disableControl()\n\n    def disableActorControlAt(self, index):\n        self.getActorControlAt(index).disableControl()\n\n    def displayFrameCounts(self):\n        for actorControl in self.actorControlList:\n            actorControl.displayFrameCounts()\n\n    def displaySeconds(self):\n        for actorControl in self.actorControlList:\n            actorControl.displaySeconds()\n\n    def resetAllToZero(self):\n        for actorControl in self.actorControlList:\n            actorControl.resetToZero()\n\n    def resetAllToEnd(self):\n        for actorControl in self.actorControlList:\n            actorControl.resetToEnd()\n\n    def nextFrame(self):\n        for actorControl in self.actorControlList:\n            actorControl.nextFrame()",
  "metadata": {
    "doc_type": "python",
    "doc_id": "doc_61",
    "parent": "processed_documents/context_documents/c5cd6f61_AnimPanel.json",
    "chunk_number": 992
  }
}