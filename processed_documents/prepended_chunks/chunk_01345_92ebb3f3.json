{
  "content": "This code chunk is part of the `setImage` method in the `OnscreenImage` class, which handles loading and displaying images in Panda3D applications. The specific section deals with cases where the input image parameter is a tuple containing a model file path and a node name. It attempts to load the model from the specified file and extract the node for display. If either the model or the node cannot be found, appropriate error messages are printed.\n\nif model:\n                node = model.find(image[1])\n                if node:\n                    self.assign(node.copyTo(parent, sort))\n                else:\n                    print('OnscreenImage: node %s not found' % image[1])\n            else:\n                print('OnscreenImage: model %s not found' % image[0])",
  "metadata": {
    "doc_type": "python",
    "doc_id": "doc_75",
    "parent": "processed_documents/context_documents/c320ed49_OnscreenImage.json",
    "chunk_number": 1345
  }
}