{
  "content": "The provided code chunk is part of the `setOrientation` method in a custom scrollbar class that inherits from `DirectFrame`. This method handles changing the orientation of the scrollbar between horizontal and vertical directions. It adjusts frame sizes for buttons to maintain custom dimensions when switching orientations and sets the appropriate axis direction for the GUI element.\n\n```python\nclass CustomScrollbar(DirectFrame):\n    \"\"\"\n    A customizable scrollbar component with adjustable orientation.\n    \n    Methods:\n        setOrientation: Changes the scrollbar's orientation, handling size adjustments.\n    \"\"\"\n\n    def __init__(self, parent=None, **kwargs):\n        # Initialization code...\n        pass\n\n    def setOrientation(self):\n        \"\"\"\n        Adjusts the scrollbar's orientation and associated button sizes.\n\n        Handles swapping frame dimensions when changing between horizontal\n        and vertical orientations. Sets the correct axis direction for \n        the GUI element.\n        \"\"\"\n        # Orientation adjustment code as in the chunk...\n```\n\ndef setOrientation(self):\n        if self['orientation'] == DGG.HORIZONTAL:\n            if self._lastOrientation in (DGG.VERTICAL, DGG.VERTICAL_INVERTED):\n                fpre = self['frameSize']\n                # swap frameSize width and height to keep custom frameSizes\n                self['frameSize'] = (fpre[2], fpre[3], fpre[0], fpre[1])\n                f = self.decButton['frameSize']\n                self.decButton['frameSize'] = (f[2], f[3], f[0], f[1])\n                f = self.incButton['frameSize']\n                self.incButton['frameSize'] = (f[2], f[3], f[0], f[1])\n            self.guiItem.setAxis(Vec3(1, 0, 0))\n        elif self['orientation'] == DGG.VERTICAL:\n            if self._lastOrientation == DGG.HORIZONTAL:\n                fpre = self['frameSize']\n                # swap frameSize width and height to keep custom frameSizes\n                self['frameSize'] = (fpre[2], fpre[3], fpre[0], fpre[1])\n                f = self.decButton['frameSize']",
  "metadata": {
    "doc_type": "python",
    "doc_id": "doc_88",
    "parent": "processed_documents/context_documents/c8becc07_DirectScrollBar.json",
    "chunk_number": 1540
  }
}